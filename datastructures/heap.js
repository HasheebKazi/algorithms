export class MaxHeap {
    constructor(arr) {
        this.heap = this.createHeap(arr);
    }

    createHeap(arr) {
        return arr;
    }

    maxHeapify(arr) {
        return arr;
    }

    poll() {
        if (this.heap.length > 0) {
            return this.heap[0];
        } else {
            return null
        }
    }

    insert() {

    }

    removeRoot() {

    }

    
}                                                                                             